(()=>{'use strict';
const SOURCE='https://cdn.jsdelivr.net/gh/aseel90/FeatherFury-LaB@5b83840d68ad65939b8efae336afd76c47b7bdc1/game.js';
try{
  const x=new XMLHttpRequest();x.open('GET',SOURCE+'?w3-clean=1',false);x.send(null);
  if(x.status<200||x.status>=400)throw new Error('stable runtime '+x.status);
  let s=x.responseText,changes=0;
  const swap=(a,b,label)=>{if(!s.includes(a))throw new Error('W3 clean transform missing: '+label);s=s.replace(a,b);changes++;};
  swap(`    const grad = this.ctx.createLinearGradient(0,0,0,CONFIG.CANVAS_HEIGHT);\n    grad.addColorStop(0, topC); grad.addColorStop(1, botC);\n    this.ctx.fillStyle = grad; this.ctx.fillRect(0,0,CONFIG.CANVAS_WIDTH,CONFIG.CANVAS_HEIGHT);\n    \n    if (this.lightning > 0) {\n      this.ctx.fillStyle = \`rgba(255,255,255,\${this.lightning * 0.5})\`;\n      this.ctx.fillRect(0,0,CONFIG.CANVAS_WIDTH,CONFIG.CANVAS_HEIGHT);\n    }\n\n    this.ctx.fillStyle = this.boss.active || this.state === 'BOSS_OUTRO' || this.state === 'FLY_AWAY' ? '#f1c40f' : '#fdfd96';\n    this.ctx.beginPath(); this.ctx.arc(CONFIG.CANVAS_WIDTH - 60, 80, 30, 0, Math.PI*2); this.ctx.fill();\n`,
`    if (this.activeWorld === 2) {\n      if (typeof this.drawWorld3Background === 'function') this.drawWorld3Background(this.ctx, this.frame);\n      else { this.ctx.fillStyle = '#05050d'; this.ctx.fillRect(0,0,CONFIG.CANVAS_WIDTH,CONFIG.CANVAS_HEIGHT); }\n    } else {\n      const grad = this.ctx.createLinearGradient(0,0,0,CONFIG.CANVAS_HEIGHT);\n      grad.addColorStop(0, topC); grad.addColorStop(1, botC);\n      this.ctx.fillStyle = grad; this.ctx.fillRect(0,0,CONFIG.CANVAS_WIDTH,CONFIG.CANVAS_HEIGHT);\n\n      this.ctx.fillStyle = this.boss.active || this.state === 'BOSS_OUTRO' || this.state === 'FLY_AWAY' ? '#f1c40f' : '#fdfd96';\n      this.ctx.beginPath(); this.ctx.arc(CONFIG.CANVAS_WIDTH - 60, 80, 30, 0, Math.PI*2); this.ctx.fill();\n    }\n\n    if (this.lightning > 0) {\n      this.ctx.fillStyle = \`rgba(255,255,255,\${this.lightning * 0.5})\`;\n      this.ctx.fillRect(0,0,CONFIG.CANVAS_WIDTH,CONFIG.CANVAS_HEIGHT);\n    }\n`,'background');
  swap(`    if (this.activeWorld === 0 || this.currentWorldIndex === 0) {\n        this.drawRuinsBackground();\n    } else if (this.currentWorldIndex === 2 || this.currentWorldIndex === 'ENDLESS') {\n        this.drawDarkForest();\n    }\n`,
`    if (this.activeWorld === 0 || this.currentWorldIndex === 0) {\n        this.drawRuinsBackground();\n    } else if (this.activeWorld !== 2 && (this.currentWorldIndex === 2 || this.currentWorldIndex === 'ENDLESS')) {\n        this.drawDarkForest();\n    }\n`,'dark-forest');
  swap(`    // Ground Colors\n    let gBase = '#3d2b1f', gTop = '#5c4033', gLines = '#8b5a2b';\n    if (this.activeWorld === 2) {\n      gBase = '#090d16'; gTop = '#1e1b4b'; gLines = '#38bdf8'; // Storm Cyber Ground\n    } else if (this.activeWorld === 1) {\n      gBase = '#94a3b8'; gTop = '#e2e8f0'; gLines = '#cbd5e1'; // Snow Ground\n    } else if (this.boss.active || this.state === 'BOSS_OUTRO' || this.state === 'FLY_AWAY') {\n      gBase = '#1e293b'; gTop = '#334155'; gLines = '#475569'; // Boss W1 Ground\n    }\n\n    this.ctx.fillStyle = gBase; this.ctx.fillRect(0, gY, CONFIG.CANVAS_WIDTH, CONFIG.GROUND_HEIGHT);\n    this.ctx.fillStyle = gTop; this.ctx.fillRect(0, gY, CONFIG.CANVAS_WIDTH, 10);\n    this.ctx.fillStyle = gLines;\n    this.ctx.beginPath();\n    for (let x = -this.groundOffset; x < CONFIG.CANVAS_WIDTH + 40; x += 30) {\n      this.ctx.moveTo(x, gY+10); this.ctx.lineTo(x+15, gY+10); this.ctx.lineTo(x-5, CONFIG.CANVAS_HEIGHT); this.ctx.lineTo(x-20, CONFIG.CANVAS_HEIGHT);\n    }\n    this.ctx.fill();\n`,
`    // Ground. World 3 owns its environment renderer completely; legacy cyber stripes are isolated.\n    if (this.activeWorld === 2) {\n      if (typeof this.drawWorld3Ground === 'function') this.drawWorld3Ground(this.ctx, this.frame);\n      else { this.ctx.fillStyle = '#090a12'; this.ctx.fillRect(0, gY, CONFIG.CANVAS_WIDTH, CONFIG.GROUND_HEIGHT); }\n    } else {\n      let gBase = '#3d2b1f', gTop = '#5c4033', gLines = '#8b5a2b';\n      if (this.activeWorld === 1) {\n        gBase = '#94a3b8'; gTop = '#e2e8f0'; gLines = '#cbd5e1';\n      } else if (this.boss.active || this.state === 'BOSS_OUTRO' || this.state === 'FLY_AWAY') {\n        gBase = '#1e293b'; gTop = '#334155'; gLines = '#475569';\n      }\n      this.ctx.fillStyle = gBase; this.ctx.fillRect(0, gY, CONFIG.CANVAS_WIDTH, CONFIG.GROUND_HEIGHT);\n      this.ctx.fillStyle = gTop; this.ctx.fillRect(0, gY, CONFIG.CANVAS_WIDTH, 10);\n      this.ctx.fillStyle = gLines;this.ctx.beginPath();\n      for (let x = -this.groundOffset; x < CONFIG.CANVAS_WIDTH + 40; x += 30) {this.ctx.moveTo(x,gY+10);this.ctx.lineTo(x+15,gY+10);this.ctx.lineTo(x-5,CONFIG.CANVAS_HEIGHT);this.ctx.lineTo(x-20,CONFIG.CANVAS_HEIGHT);}\n      this.ctx.fill();\n    }\n`,'ground');
  swap(`        if (this.state === 'STORY') {\n          if (this.activeWorld === 0) this.drawCage();\n          else this.drawBlizzardIntro();\n        }\n`,
`        if (this.state === 'STORY') {\n          if (this.activeWorld === 0) this.drawCage();\n          else if (this.activeWorld === 1) this.drawBlizzardIntro();\n        }\n`,'story-weather');

  swap(`    if (this.state === 'BOSS_OUTRO' || this.state === 'FLY_AWAY') {
      if (this.activeWorld === 2) this.drawPhoenix(this.ctx, this.owl.x, this.owl.y, this.frame);
      else if (this.activeWorld === 1) this.drawEagle(this.ctx, this.owl.x, this.owl.y, this.frame);
      else this.drawOwl(this.ctx, this.owl.x, this.owl.y, this.frame);
    }
`,
`    if (this.state === 'BOSS_OUTRO' || this.state === 'FLY_AWAY') {
      if (this.activeWorld === 2) {
        if (typeof window.drawBirdSkin === 'function') {
          window.drawBirdSkin(this.ctx, 'phoenix', this.owl.x, this.owl.y, -0.04, Math.sin(this.frame * 0.42), 1.18, false);
        } else {
          this.drawPhoenix(this.ctx, this.owl.x, this.owl.y, this.frame);
        }
      } else if (this.activeWorld === 1) this.drawEagle(this.ctx, this.owl.x, this.owl.y, this.frame);
      else this.drawOwl(this.ctx, this.owl.x, this.owl.y, this.frame);
    }
`,'w3-outro-phoenix-skin');

  swap(`      if (this.activeWorld === 2) {
        document.getElementById('endGameTitle').textContent = I18N[this.lang].w3_winText;
        this.unlockedSkins.add('eagle');
        safeSet('fh_unlocked_skins', JSON.stringify([...this.unlockedSkins]));
      } else if (this.activeWorld === 1) {
`,
`      if (this.activeWorld === 2) {
        document.getElementById('endGameTitle').textContent = I18N[this.lang].w3_winText;
        this.unlockedSkins.add('phoenix');
        safeSet('fh_unlocked_skins', JSON.stringify([...this.unlockedSkins]));
      } else if (this.activeWorld === 1) {
`,'w3-phoenix-reward');

  swap("  generateStormPillar() {\n    const c = document.createElement('canvas');\n    c.width = 80; c.height = 600;\n    const ctx = c.getContext('2d');\n    \n    // Dark Metallic Cyber-Obsidian Body\n    const grad = ctx.createLinearGradient(0, 0, 80, 0);\n    grad.addColorStop(0, '#090d16');\n    grad.addColorStop(0.25, '#1e1b4b');\n    grad.addColorStop(0.5, '#312e81');\n    grad.addColorStop(0.75, '#1e1b4b');\n    grad.addColorStop(1, '#090d16');\n    ctx.fillStyle = grad;\n    ctx.fillRect(10, 0, 60, 600);\n    \n    // Tech Plate Panel Ridges\n    ctx.strokeStyle = '#4338ca';\n    ctx.lineWidth = 1.5;\n    for(let y = 30; y < 600; y += 45) {\n      ctx.strokeRect(14, y, 52, 38);\n      ctx.fillStyle = '#6366f1';\n      ctx.fillRect(16, y + 2, 3, 3);\n      ctx.fillRect(61, y + 2, 3, 3);\n    }\n    \n    // Central High-Voltage Neon Lightning Conduit\n    ctx.shadowBlur = 18;\n    ctx.shadowColor = '#38bdf8';\n    \n    // Pulsing Outer Cyan Conduit\n    ctx.lineWidth = 4;\n    ctx.strokeStyle = '#0284c7';\n    ctx.beginPath();\n    ctx.moveTo(40, 0);\n    for(let y = 0; y <= 600; y += 25) {\n      const x = 40 + (Math.random() - 0.5) * 16;\n      ctx.lineTo(x, y);\n    }\n    ctx.stroke();\n    \n    // Bright Core Lightning Beam\n    ctx.lineWidth = 2;\n    ctx.strokeStyle = '#38bdf8';\n    ctx.stroke();\n    ctx.lineWidth = 1;\n    ctx.strokeStyle = '#ffffff';\n    ctx.stroke();\n    \n    // Tesla Arc Branches\n    ctx.shadowColor = '#c084fc';\n    ctx.strokeStyle = '#c084fc';\n    ctx.lineWidth = 1.5;\n    for(let i = 0; i < 10; i++) {\n      const y = 40 + Math.random() * 520;\n      ctx.beginPath();\n      ctx.moveTo(40, y);\n      ctx.lineTo(40 + (Math.random() > 0.5 ? 22 : -22), y + (Math.random() - 0.5) * 30);\n      ctx.stroke();\n    }\n    ctx.shadowBlur = 0;\n    \n    // Tesla Spire Cap (Top/Bottom Terminal)\n    const capGrad = ctx.createLinearGradient(0, 0, 80, 0);\n    capGrad.addColorStop(0, '#1e1b4b');\n    capGrad.addColorStop(0.5, '#4338ca');\n    capGrad.addColorStop(1, '#1e1b4b');\n    ctx.fillStyle = capGrad;\n    ctx.fillRect(2, 0, 76, 26);\n    \n    // Cap Tesla Rings\n    ctx.fillStyle = '#38bdf8';\n    ctx.shadowBlur = 12; ctx.shadowColor = '#38bdf8';\n    ctx.fillRect(8, 22, 64, 4);\n    ctx.fillStyle = '#c084fc';\n    ctx.shadowColor = '#c084fc';\n    ctx.fillRect(16, 12, 48, 3);\n    ctx.shadowBlur = 0;\n    \n    // Metallic Rim & Bolts\n    ctx.fillStyle = '#94a3b8';\n    ctx.fillRect(4, 2, 72, 3);\n    for(let bx = 12; bx <= 68; bx += 14) {\n      ctx.fillStyle = '#f8fafc';\n      ctx.fillRect(bx, 6, 4, 4);\n    }\n    \n    this.assets.stormPillarCanvas = c;\n  }", "  generateStormPillar() {\n    const makeStormPillar = (variant) => {\n      const c = document.createElement('canvas');\n      c.width = 80; c.height = 600;\n      const ctx = c.getContext('2d');\n      const spec = variant === 'charged'\n        ? { bodyX:9, bodyW:62, side:'#5b21b6', edge:'#7c3aed', core:'#67e8f9', accent:'#c084fc', panel:'#4c1d95', cap:'#6d28d9', spacing:42 }\n        : variant === 'heavy'\n          ? { bodyX:6, bodyW:68, side:'#312e81', edge:'#6366f1', core:'#60a5fa', accent:'#a78bfa', panel:'#27245f', cap:'#3730a3', spacing:52 }\n          : { bodyX:11, bodyW:58, side:'#1e1b4b', edge:'#4f46e5', core:'#38bdf8', accent:'#8b5cf6', panel:'#252552', cap:'#4338ca', spacing:47 };\n\n      const grad = ctx.createLinearGradient(0, 0, 80, 0);\n      grad.addColorStop(0, '#060812');\n      grad.addColorStop(.18, spec.side);\n      grad.addColorStop(.50, '#171a35');\n      grad.addColorStop(.82, spec.side);\n      grad.addColorStop(1, '#060812');\n      ctx.fillStyle = grad;\n      ctx.fillRect(spec.bodyX, 0, spec.bodyW, 600);\n\n      // Reinforced side rails.\n      ctx.fillStyle = '#0a0d19';\n      ctx.fillRect(spec.bodyX, 0, 5, 600);\n      ctx.fillRect(spec.bodyX + spec.bodyW - 5, 0, 5, 600);\n      ctx.strokeStyle = spec.edge;\n      ctx.lineWidth = variant === 'heavy' ? 2.2 : 1.5;\n      ctx.beginPath();\n      ctx.moveTo(spec.bodyX + 5, 0); ctx.lineTo(spec.bodyX + 5, 600);\n      ctx.moveTo(spec.bodyX + spec.bodyW - 5, 0); ctx.lineTo(spec.bodyX + spec.bodyW - 5, 600);\n      ctx.stroke();\n\n      // Modular armour plates.\n      for (let y = 34, i = 0; y < 600; y += spec.spacing, i++) {\n        const inset = variant === 'heavy' ? 10 : 12;\n        const ph = Math.min(spec.spacing - 9, 39);\n        ctx.fillStyle = i % 2 ? '#11152b' : spec.panel;\n        ctx.fillRect(spec.bodyX + inset, y, spec.bodyW - inset * 2, ph);\n        ctx.strokeStyle = 'rgba(148,163,184,.34)';\n        ctx.lineWidth = 1;\n        ctx.strokeRect(spec.bodyX + inset + .5, y + .5, spec.bodyW - inset * 2 - 1, ph - 1);\n        ctx.fillStyle = spec.edge;\n        ctx.fillRect(spec.bodyX + inset + 3, y + 4, 3, 3);\n        ctx.fillRect(spec.bodyX + spec.bodyW - inset - 6, y + 4, 3, 3);\n\n        // Gothic diagonal brace etched into each plate.\n        ctx.strokeStyle = variant === 'charged' ? 'rgba(192,132,252,.35)' : 'rgba(99,102,241,.26)';\n        ctx.beginPath();\n        if (i % 2) {\n          ctx.moveTo(spec.bodyX + inset + 8, y + ph - 6);\n          ctx.lineTo(spec.bodyX + spec.bodyW - inset - 8, y + 7);\n        } else {\n          ctx.moveTo(spec.bodyX + inset + 8, y + 7);\n          ctx.lineTo(spec.bodyX + spec.bodyW - inset - 8, y + ph - 6);\n        }\n        ctx.stroke();\n      }\n\n      // Recessed central energy channel. The animated energy itself is drawn at runtime.\n      ctx.fillStyle = '#050814';\n      ctx.fillRect(35, 0, 10, 600);\n      ctx.fillStyle = spec.core;\n      ctx.globalAlpha = variant === 'charged' ? .54 : .34;\n      ctx.fillRect(39, 0, 2, 600);\n      ctx.globalAlpha = 1;\n\n      // Terminal cap at the gap edge.\n      const capGrad = ctx.createLinearGradient(0, 0, 80, 0);\n      capGrad.addColorStop(0, '#0b1020');\n      capGrad.addColorStop(.24, spec.cap);\n      capGrad.addColorStop(.5, '#11152f');\n      capGrad.addColorStop(.76, spec.cap);\n      capGrad.addColorStop(1, '#0b1020');\n      ctx.fillStyle = capGrad;\n      ctx.beginPath();\n      ctx.moveTo(2, 29); ctx.lineTo(8, 3); ctx.lineTo(72, 3); ctx.lineTo(78, 29);\n      ctx.closePath(); ctx.fill();\n      ctx.strokeStyle = '#64748b'; ctx.lineWidth = 1.5; ctx.stroke();\n\n      // Cap armour, conductor ring and bolts.\n      ctx.fillStyle = '#94a3b8';\n      ctx.fillRect(8, 4, 64, 3);\n      ctx.fillStyle = spec.accent;\n      ctx.fillRect(16, 13, 48, variant === 'heavy' ? 4 : 3);\n      ctx.fillStyle = spec.core;\n      ctx.fillRect(8, 24, 64, 4);\n      ctx.fillStyle = '#e2e8f0';\n      for (let bx = 13; bx <= 67; bx += 13.5) ctx.fillRect(bx, 8, 3, 3);\n\n      // Small side fins give the silhouette a Storm Spire profile without changing collision.\n      ctx.fillStyle = '#11162a';\n      ctx.beginPath(); ctx.moveTo(8, 10); ctx.lineTo(1, 24); ctx.lineTo(10, 21); ctx.closePath(); ctx.fill();\n      ctx.beginPath(); ctx.moveTo(72, 10); ctx.lineTo(79, 24); ctx.lineTo(70, 21); ctx.closePath(); ctx.fill();\n\n      return c;\n    };\n\n    this.assets.stormPillarCanvases = {\n      standard: makeStormPillar('standard'),\n      charged: makeStormPillar('charged'),\n      heavy: makeStormPillar('heavy')\n    };\n    this.assets.stormPillarCanvas = this.assets.stormPillarCanvases.standard;\n  }",'storm-pillar-builder-v2');
  swap("  drawPillars() {\n    const pCanvas = this.activeWorld === 2 ? this.assets.stormPillarCanvas : (this.activeWorld === 1 ? this.assets.icePillarCanvas : this.assets.pillarCanvas);\n    \n    this.pillars.forEach(p => {\n      if (p.smashed) return;\n\n      if (pCanvas) {\n        let gap = this.activeWorld === 2 ? CONFIG.W3_GAP_SIZE : this.activeWorld === 1 ? CONFIG.W2_GAP_SIZE : CONFIG.GAP_SIZE;\n        const botY = p.topHeight + gap;\n        const botH = CONFIG.CANVAS_HEIGHT - CONFIG.GROUND_HEIGHT - botY;\n        \n        // We draw the pillar slightly wider than the collision box so the cap overhangs nicely\n        const drawW = 80;\n        const drawX = p.x - (drawW - p.width) / 2; // centered\n        \n        // --- TOP PILLAR ---\n        // Cap is at the gap. We flip the canvas vertically.\n        this.ctx.save();\n        this.ctx.translate(drawX, p.topHeight); // Move to gap\n        this.ctx.scale(1, -1);\n        // Draw starting from 0, going up (which goes down visually due to scale)\n        this.ctx.drawImage(pCanvas, 0, 0, 80, Math.min(600, p.topHeight), 0, 0, drawW, p.topHeight);\n        this.ctx.restore();\n\n        // --- BOTTOM PILLAR ---\n        // Cap is at the gap. No flip needed.\n        this.ctx.drawImage(pCanvas, 0, 0, 80, Math.min(600, botH), drawX, botY, drawW, botH);\n      }\n    });\n  }", "  drawPillars() {\n    this.pillars.forEach(p => {\n      if (p.smashed) return;\n\n      const isStorm = this.activeWorld === 2;\n      let pCanvas;\n      if (isStorm) {\n        const bank = this.assets.stormPillarCanvases || {};\n        const stage2 = Number(this.score || 0) >= Number(CONFIG.STAGE1_END || 15);\n        const fallbackVariant = stage2 ? 'charged' : 'standard';\n        const variant = p.__w3Variant || fallbackVariant;\n        pCanvas = bank[variant] || this.assets.stormPillarCanvas;\n      } else {\n        pCanvas = this.activeWorld === 1 ? this.assets.icePillarCanvas : this.assets.pillarCanvas;\n      }\n\n      if (!pCanvas) return;\n\n      const gap = isStorm ? CONFIG.W3_GAP_SIZE : this.activeWorld === 1 ? CONFIG.W2_GAP_SIZE : CONFIG.GAP_SIZE;\n      const botY = p.topHeight + gap;\n      const botH = CONFIG.CANVAS_HEIGHT - CONFIG.GROUND_HEIGHT - botY;\n      const drawW = 80;\n      const drawX = p.x - (drawW - p.width) / 2;\n\n      // Top pillar: flip the modular canvas so its terminal faces the gap.\n      this.ctx.save();\n      this.ctx.translate(drawX, p.topHeight);\n      this.ctx.scale(1, -1);\n      this.ctx.drawImage(pCanvas, 0, 0, 80, Math.min(600, p.topHeight), 0, 0, drawW, p.topHeight);\n      this.ctx.restore();\n\n      // Bottom pillar.\n      this.ctx.drawImage(pCanvas, 0, 0, 80, Math.min(600, botH), drawX, botY, drawW, botH);\n\n      if (!isStorm) return;\n\n      const variant = p.__w3Variant || (Number(this.score || 0) >= Number(CONFIG.STAGE1_END || 15) ? 'charged' : 'standard');\n      const seed = Number(p.__w3EnergySeed || 0);\n      const charged = variant === 'charged';\n      const heavy = variant === 'heavy';\n      const enraged = !!(this.boss && this.boss.active && this.boss.enraged);\n      const lightningBoost = Number(this.lightning || 0) > 0 ? .55 : 0;\n      const pulse = .5 + .5 * Math.sin(this.frame * (charged ? .22 : .16) + seed * .09);\n      const energy = Math.min(1, .34 + pulse * (charged ? .50 : heavy ? .34 : .28) + lightningBoost + (enraged ? .18 : 0));\n      const core = charged || enraged ? '#67e8f9' : '#38bdf8';\n      const accent = charged || enraged ? '#c084fc' : '#8b5cf6';\n      const speed = charged ? 3.6 : heavy ? 2.4 : 2.9;\n\n      const drawEnergyRun = (y0, hh, upward) => {\n        if (hh <= 4) return;\n        this.ctx.save();\n        this.ctx.beginPath();\n        this.ctx.rect(drawX + 5, y0, drawW - 10, hh);\n        this.ctx.clip();\n        this.ctx.globalCompositeOperation = 'lighter';\n        this.ctx.lineCap = 'round';\n\n        const travel = (this.frame * speed + seed * 4) % 64;\n        for (let i = -1; i < Math.ceil(hh / 64) + 2; i++) {\n          const local = i * 64 + travel;\n          const py = upward ? y0 + hh - local : y0 + local;\n          if (py < y0 - 14 || py > y0 + hh + 14) continue;\n\n          this.ctx.shadowColor = core;\n          this.ctx.shadowBlur = 8 + 10 * energy;\n          this.ctx.strokeStyle = 'rgba(103,232,249,' + (.20 + energy * .58) + ')';\n          this.ctx.lineWidth = charged ? 3.2 : 2.5;\n          this.ctx.beginPath();\n          this.ctx.moveTo(drawX + 40, py - 11);\n          this.ctx.lineTo(drawX + 36 + Math.sin((py + seed) * .11) * 3, py - 3);\n          this.ctx.lineTo(drawX + 43 + Math.cos((py + seed) * .09) * 3, py + 5);\n          this.ctx.lineTo(drawX + 40, py + 13);\n          this.ctx.stroke();\n\n          this.ctx.shadowColor = accent;\n          this.ctx.shadowBlur = 7 + 7 * energy;\n          this.ctx.fillStyle = 'rgba(192,132,252,' + (.14 + energy * .42) + ')';\n          this.ctx.fillRect(drawX + 16, py - 1, 10 + (heavy ? 8 : 3), 2);\n          this.ctx.fillRect(drawX + 52 - (heavy ? 8 : 3), py + 4, 11 + (heavy ? 5 : 0), 2);\n        }\n        this.ctx.restore();\n      };\n\n      drawEnergyRun(0, p.topHeight, true);\n      drawEnergyRun(botY, botH, false);\n\n      // Gap terminals pulse in sync with the storm and react to lightning.\n      this.ctx.save();\n      this.ctx.globalCompositeOperation = 'lighter';\n      this.ctx.shadowColor = core;\n      this.ctx.shadowBlur = 9 + energy * 14;\n      this.ctx.strokeStyle = 'rgba(103,232,249,' + (.35 + energy * .55) + ')';\n      this.ctx.lineWidth = charged ? 3 : 2;\n      this.ctx.beginPath();\n      this.ctx.moveTo(drawX + 9, p.topHeight - 2); this.ctx.lineTo(drawX + 71, p.topHeight - 2);\n      this.ctx.moveTo(drawX + 9, botY + 2); this.ctx.lineTo(drawX + 71, botY + 2);\n      this.ctx.stroke();\n\n      // Deterministic cap sparks; no frame-to-frame random jitter.\n      const sparkPhase = (this.frame + seed * 7) % (charged ? 18 : 27);\n      if (sparkPhase < (charged ? 7 : 4)) {\n        const sway = Math.sin(this.frame * .31 + seed) * 6;\n        this.ctx.strokeStyle = 'rgba(216,180,254,' + (.45 + energy * .45) + ')';\n        this.ctx.lineWidth = 1.4;\n        this.ctx.shadowColor = accent;\n        this.ctx.shadowBlur = 8;\n        this.ctx.beginPath();\n        this.ctx.moveTo(drawX + 18, p.topHeight - 4);\n        this.ctx.lineTo(drawX + 25 + sway, p.topHeight - 12);\n        this.ctx.lineTo(drawX + 31 + sway * .45, p.topHeight - 5);\n        this.ctx.moveTo(drawX + 62, botY + 4);\n        this.ctx.lineTo(drawX + 55 - sway, botY + 13);\n        this.ctx.lineTo(drawX + 49 - sway * .4, botY + 6);\n        this.ctx.stroke();\n      }\n      this.ctx.restore();\n    });\n  }",'storm-pillar-renderer-v2');

  window.__FF_W3_LEGACY_ENVIRONMENT_ISOLATED__={version:'1.2.0',changes,phoenixOutro:true,phoenixReward:true,pillarSystem:'storm-spire-v2'};
  (0,eval)(s+'\n//# sourceURL=ff-stable-runtime-w3-clean-v1.js');
}catch(e){console.error('[FeatherFury] clean stable runtime failed',e);throw e;}
})();